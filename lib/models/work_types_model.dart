// A class to represent different types of work
class WorkTypes {
  // The image associated with the work type
  final String image;

  // The text description of the work type
  final String text;

  // Constructor to create a new WorkTypes object
  WorkTypes({
    // Required parameters
    required this.image,
    required this.text,
  });
}
